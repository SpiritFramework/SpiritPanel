import { ServerNetworkDashboard } from '../../components/server/network/ServerNetworkDashboard';
import { ServerPage } from '../../components/server/ServerPage';

export function ServerNetworkPage() {
  return (
    <ServerPage>
      <ServerNetworkDashboard />
    </ServerPage>
  );
}
