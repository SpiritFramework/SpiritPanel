import { ServerActivityDashboard } from '../../components/server/activity/ServerActivityDashboard';
import { ServerPage } from '../../components/server/ServerPage';

export function ServerActivityPage() {
  return (
    <ServerPage>
      <ServerActivityDashboard />
    </ServerPage>
  );
}
