import { ServerStartupDashboard } from '../../components/server/startup/ServerStartupDashboard';
import { ServerPage } from '../../components/server/ServerPage';

export function ServerStartupPage() {
  return (
    <ServerPage>
      <ServerStartupDashboard />
    </ServerPage>
  );
}
