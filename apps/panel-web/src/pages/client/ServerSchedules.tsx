import { ServerSchedulesDashboard } from '../../components/server/schedules/ServerSchedulesDashboard';
import { ServerPage } from '../../components/server/ServerPage';

export function ServerSchedulesPage() {
  return (
    <ServerPage>
      <ServerSchedulesDashboard />
    </ServerPage>
  );
}
