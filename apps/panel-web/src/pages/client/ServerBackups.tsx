import { ServerBackupsDashboard } from '../../components/server/backups/ServerBackupsDashboard';
import { ServerPage } from '../../components/server/ServerPage';

export function ServerBackupsPage() {
  return (
    <ServerPage>
      <ServerBackupsDashboard />
    </ServerPage>
  );
}
